{% extends 'admin/_layouts/after-login.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('admin_default') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('admin_default') }}
{% endblock %}

{% block title %}Szczegóły ogłoszenia{% endblock %}

{% block body %}

    {{ website_path([
        { label: 'Ustawienia'|trans, route: 'admin_settings' },
        { label: 'Ogłoszenia'|trans, route: 'admin_announcement' },
        { label: 'Szczegóły ogłoszenia'|trans },
    ]) }}

    <section class="bg-white p-4 rounded-lg dark:bg-zinc-900">
        <div class="max-w-4xl">
            {# Główny header #}
            <header class="mb-4">
                <div class="mt-2 flex flex-wrap items-center gap-x-4 gap-y-1 text-sm text-zinc-600 dark:text-zinc-300">
                    <div class="flex items-center gap-2">
                        <span class="font-medium text-zinc-900 dark:text-white">{% trans %}Data publikacji{% endtrans %}</span>
                        <span>{{ (announcement.startsAt ?? announcement.createdAt)|date('d.m.Y H:i') }}</span>
                    </div>
                </div>
                <br>
                <div class="flex items-start justify-between gap-3">
                    <h1 class="text-2xl font-bold tracking-tight text-zinc-900 dark:text-white">
                        {{ announcement.title }}
                    </h1>
                </div>
                <div class="mt-2 flex flex-wrap items-center gap-x-4 gap-y-1 text-sm text-zinc-600 dark:text-zinc-300">
                    {#                    <div class="flex items-center gap-2"> #}
                    {#                        <span class="font-medium text-zinc-900 dark:text-white">Data publikacji</span> #}
                    {#                        <span>{{ (announcement.startsAt ?? announcement.createdAt)|date('d.m.Y H:i') }}</span> #}
                    {#                    </div> #}
                    {#                    {% if announcement.author is defined and announcement.author %} #}
                    {#                        <div class="hidden sm:block h-4 w-px bg-zinc-200 dark:bg-zinc-700"></div> #}
                    {#                        <div class="flex items-center gap-2"> #}
                    {#                            <span class="font-medium text-zinc-900 dark:text-white">Autor</span> #}
                    {#                            <span>{{ announcement.author }}</span> #}
                    {#                        </div> #}
                    {#                    {% endif %} #}
                    {#                    {% if announcement.category is defined and announcement.category %} #}
                    {#                        <div class="hidden sm:block h-4 w-px bg-zinc-200 dark:bg-zinc-700"></div> #}
                    {#                        <div> #}
                    {#            <span class="inline-flex items-center rounded-full bg-zinc-100 px-2.5 py-0.5 text-xs font-medium text-zinc-700 dark:bg-zinc-800 dark:text-zinc-200"> #}
                    {#              {{ announcement.category }} #}
                    {#            </span> #}
                    {#                        </div> #}
                    {#                    {% endif %} #}
                </div>
            </header>

            {# Karta treści #}
            <article class="rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-700 dark:bg-zinc-800">
                <div class="prose max-w-none prose-zinc dark:prose-invert">
                    {# Treść ogłoszenia – HTML z bazy #}
                    {{ announcement.description|raw }}
                </div>

                {# Załączniki #}
                {#                {% if announcement.attachments is defined and announcement.attachments|length %} #}
                {#                    <div class="mt-6"> #}
                {#                        <h2 class="text-sm font-semibold text-zinc-900 dark:text-white mb-3">Załączniki</h2> #}
                {#                        <ul class="space-y-2"> #}
                {#                            {% for file in announcement.attachments %} #}
                {#                                <li> #}
                {#                                    <a href="{{ path('attachment_download', { id: file.id }) }}" #}
                {#                                        class="inline-flex items-center gap-2 rounded-md border border-zinc-200 bg-white px-3 py-2 text-sm text-zinc-900 hover:bg-zinc-50 dark:border-zinc-700 dark:bg-zinc-700 dark:text-white dark:hover:bg-zinc-600"> #}
                {#                                        <svg class="h-4 w-4" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 24 24"> #}
                {#                                            <path d="M12 3a1 1 0 0 1 1 1v10.586l3.293-3.293a1 1 0 1 1 1.414 1.414l-5 5a1 1 0 0 1-1.414 0l-5-5a1 1 0 1 1 1.414-1.414L11 14.586V4a1 1 0 0 1 1-1Z" /> #}
                {#                                        </svg> #}
                {#                                        {{ file.originalName ?? ('Plik #' ~ loop.index) }} #}
                {#                                    </a> #}
                {#                                </li> #}
                {#                            {% endfor %} #}
                {#                        </ul> #}
                {#                    </div> #}
                {#                {% endif %} #}

                {# CTA / akcje #}
            </article>

        </div>
    </section>
{% endblock %}
