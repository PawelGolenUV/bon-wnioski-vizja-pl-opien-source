framework:
    rate_limiter:
        guest_create_account:
            policy: 'sliding_window'
            limit: 3
            interval: '60 minutes'

        password_reset_request:
            policy: 'sliding_window'
            limit: 3
            interval: '60 minutes'
